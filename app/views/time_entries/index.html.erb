<h2><%= @project.name %></h2>
<ul>
<% @entries.each do |entry| %>
	<li>
		<%= link_to "Edit", project_time_entry_path(@project, entry) %>
		<strong>Length:</strong> <%= entry.hours %> h <%= entry.minutes %> m, 
		<strong>Info:</strong> <%= entry.comment %>,
		<strong>On:</strong> <%= entry.date %>
	</li>
<% end %>
</ul>