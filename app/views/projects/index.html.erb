<table>
	<% @projects.each do |project| %>
		<tr>
			<td><%= project.name %></td>
			<% unless project.description.nil?  %>
				<td>-</td>
				<td><%= project.description %></td>
			<% end %>
		</tr>
	<% end %>
</table>
